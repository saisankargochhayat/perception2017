{% extends "::base.html.twig" %}
{% block body %}
    <section id="sub-header" >
        <div class="container">
            <div class="row">
                <div class="col-md-12 text-center">
                    <h1>Account Settings</h1>
                </div>
            </div><!-- End row -->
        </div><!-- End container -->
        <div class="divider_top"></div>
    </section>
<section>
    <div class="container">
        <div class="row">
            {{ form_start(form) }}
                {{ form_rest(form) }}
                <button class="btn btn-success">Change</button>
            <a class="btn btn-primary" href="{{ path('my_study_table') }}" >Cancel</a>
            {{ form_end(form) }}

        </div>
    </div>
</section>
{% endblock %}